现在希望重构用于 demo onboarding 的流程
首先，删除最初用于 onboarding 的连接日历、邮箱、CRM 的引导，以及“找到3个meetings”的引导，从官网可以直接进入产品里

然后在页面右上角，增加一个 dropdown menu，用来控制演示什么内容

有这几个 hero scenarios 想要 demo，作为 dropdown menu 里的 item，点击到哪个，就显示哪个场景的假数据

【会前自动准备】连接日历后，AI 识别未来里的会议主题和参会对象，然后提前做好调研、生成 agenda、关键问题和开场白
* 由于调研和内容生成需要时间，所以可以在 chat 里告诉用户说识别到了这些会议，正在为你做准备工作，然后才生成好内容

【自定义工作流】用户将历史文件、SOP 等材料发给 AI 助理，用自然语言叮嘱 AI 什么时候该怎么做；AI 根据要求将工作完成后，提醒用户来 review
* 这里还是用 chat 的方式，多提几轮要求，并展示更新 playbook

【主动识别任务】AI 主动从会议或邮件里识别调研、写作、写邮件、约会等任务，帮用户做好执行准备
* 会议结束后，AI 通过 chat 提醒用户发现了任务，同时在右侧 workpanel 里也会呈现相应的 follow-ups，点击就可以开始完成

【主动提醒风险】AI 主动分析并发现和客户互动过程里的风险点，提醒用户关注并给出解决方案
* 在 workpanel 里呈现 insights

【问答与发起任务】可以随时通过 Chat 提问，询问关于会议、客户、工作的一切，让 AI 生成或修改内容
* 在 all work 里多问几个问题，有来有回，并且有的回复不仅是纯文本，还有图片、表格或者文件块

同时保证左侧 topic 还是这么多，右侧 workpanel 里的内容也不变

对了，有几个新的改动：
1. All work 的 tab 改名为 Shadow
2. 移除侧边栏收起展开的交互
3. 产品左上角的 Shadow 删掉，直接是 Command Room
4. 用户发出去的 message 不要用黑色作为底色，用 #bccefb